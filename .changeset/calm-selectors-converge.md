---
"scheme-tokens": minor
---

Reject a separate CSS scope at compile time when exact mode selectors are supplied, matching the existing runtime contract.

Describe the package explicitly as a zero-runtime-dependency design-token graph compiler.
