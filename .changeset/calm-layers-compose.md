---
"scheme-tokens": patch
---

Preserve finite reference-key inference across heterogeneous layer tuples so graph tokens can
reference keys from every tuple member and typos remain statically rejected.
