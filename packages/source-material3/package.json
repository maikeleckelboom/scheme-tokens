{
  "name": "@scheme-tokens/source-material3",
  "version": "0.1.0",
  "description": "Material 3 source adapter for scheme-tokens.",
  "keywords": [
    "color-scheme",
    "design-tokens",
    "material-3",
    "token-graph"
  ],
  "homepage": "https://github.com/maikeleckelboom/scheme-tokens/tree/main/packages/source-material3#readme",
  "bugs": {
    "url": "https://github.com/maikeleckelboom/scheme-tokens/issues"
  },
  "license": "MIT",
  "author": "Maikel Eckelboom",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/maikeleckelboom/scheme-tokens.git",
    "directory": "packages/source-material3"
  },
  "files": [
    "dist",
    "README.md",
    "NOTICE.md",
    "LICENSE"
  ],
  "type": "module",
  "sideEffects": false,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./package.json": "./package.json"
  },
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsdown",
    "typecheck": "tsc -p tsconfig.lib.json && tsc -p tsconfig.test.json && tsc -p tsconfig.scripts.json",
    "lint": "oxlint . --import-plugin --vitest-plugin --deny-warnings",
    "test": "vitest run",
    "package:check": "pnpm build && publint --strict && attw --pack . --profile esm-only",
    "check:tarball": "node --experimental-strip-types scripts/check-tarball.ts",
    "format": "oxfmt --check .",
    "format:fix": "oxfmt --write .",
    "validate": "pnpm typecheck && pnpm lint && pnpm test && pnpm build && pnpm format",
    "release:check": "pnpm validate && pnpm package:check && pnpm check:tarball",
    "prepack": "pnpm build"
  },
  "dependencies": {
    "@material/material-color-utilities": "0.4.0"
  },
  "devDependencies": {
    "scheme-tokens": "workspace:*"
  },
  "peerDependencies": {
    "scheme-tokens": "^0.1.0"
  },
  "engines": {
    "node": ">=22"
  },
  "packageManager": "pnpm@11.7.0"
}
